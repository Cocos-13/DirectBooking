========================================================================
 DIRECT BOOKING — GO-LIVE CHECKLIST
 Patras Center Apartment · direct-booking site (Next.js + Viva + Resend)
 Work through top to bottom. [ ] = todo, [x] = done.
 Env var names match .env.local.example. Payment details are in PAYMENTS.md.
========================================================================

------------------------------------------------------------------------
STAGE 0 — ACCOUNTS YOU NEED (do these first; everything else depends on them)
------------------------------------------------------------------------
[x] 0.1  A domain name for the site (e.g. yourapartment.gr).
         Bought: cocosapartments.com (Cloudflare Registrar).
[x] 0.2  A Vercel account (or other Node host) connected to the GitHub repo
         Cocos-13/DirectBooking. Project: direct-booking. Root Directory = v2,
         Framework Preset must be explicitly "Next.js" (was stuck on "Other"
         even after selecting it at import — caused broken edge middleware
         bundling / __dirname crash; fixed by setting it manually).
[x] 0.3  A Viva.com account — start in the DEMO portal (demo.vivapayments.com);
         you'll add the LIVE one later in Stage 7.
[x] 0.4  A Resend account (resend.com) for sending emails.
[x] 0.5  An Upstash account (or use Vercel's KV/Marketplace integration).
         Done via Vercel Storage > Upstash Redis (auto-injected KV vars).

------------------------------------------------------------------------
STAGE 1 — PROVISION THE KV STORE  *** MOST IMPORTANT SINGLE STEP ***
Without this the site still runs, but you LOSE: date holds, webhook amount
verification, durable rate limiting, 12-month PII auto-purge, and the token
vault (PII stays out of URLs only with the store). Do not go live without it.
------------------------------------------------------------------------
[x] 1.1  On Vercel: Storage > add a Redis/KV store (Upstash) to the project.
         This auto-injects KV_REST_API_URL and KV_REST_API_TOKEN.
         (Elsewhere: create an Upstash Redis DB and copy its REST URL + token
          into UPSTASH_REDIS_REST_URL / UPSTASH_REDIS_REST_TOKEN.)
[x] 1.2  Confirm both variables are present in the deployment environment.

------------------------------------------------------------------------
STAGE 2 — EMAIL (Resend)
------------------------------------------------------------------------
[x] 2.1  In Resend, add and VERIFY your sending domain (DNS records).
         (Until then you can only send from onboarding@resend.dev for testing.)
         Verified: cocosapartments.com (DNS records added in Cloudflare, DNS
         only / grey-cloud, not proxied).
[x] 2.2  Create a Resend API key.
[x] 2.3  Set env vars:
           RESEND_API_KEY            = re_...
           RESEND_FROM_EMAIL         = "Patras Apartment <bookings@yourdomain.gr>"
           OWNER_NOTIFICATION_EMAIL  = your inbox (where booking/paid alerts go)
         NOTE: enter the RESEND_FROM_EMAIL value in Vercel WITHOUT surrounding
         quote marks (Vercel's env var UI stores literal characters, unlike a
         .env file — typing the quotes broke Resend's `from` field validation
         until removed).
[x] 2.4  Later, after deploy, send yourself a test to confirm delivery + that
         mails don't land in spam (SPF/DKIM from the verified domain).
         Confirmed via a real booking-request submission — owner + guest
         emails both arrived.

------------------------------------------------------------------------
STAGE 3 — CALENDAR SYNC (Airbnb / Booking.com iCal)
------------------------------------------------------------------------
[x] 3.1  Airbnb: Listing > Availability > Sync calendars > Export calendar link.
           Set  ICAL_URL_AIRBNB = that URL.
         Obtained and working — verified by fetching it: 7 events (4 "Reserved",
         3 "Airbnb (Not available)"), parses cleanly.
[x] 3.2  Booking.com: Extranet > Calendar > Sync > iCal export URL.
           Set  ICAL_URL_BOOKING = that URL.
         Obtained and working — verified: 3 "CLOSED - Not available" events.
         NOTE the host is ical.booking.com/v1/export/... (not the admin.booking
         .com URL shown in .env.local.example).

[x] 3.2b DONE (2026-07-23) — both iCal URLs added to the Vercel PRODUCTION
         environment (ICAL_URL_AIRBNB, ICAL_URL_BOOKING) and deployed. Secrets
         were piped from .env.local straight into `vercel env add` (never hit
         shell history), no surrounding quotes. The live endpoint no longer
         returns {"merged":[]}:
           curl -s https://www.cocosapartments.com/api/availability
         now returns real ranges — 2026-07-20→23, 07-25→30, 08-02→05,
         08-08→12, 08-22→24 (plus a long 2027-01-20→2028-01-23 block; see the
         live-data NOTE below).

[x] 3.3  After deploy: in BOTH Airbnb and Booking.com, IMPORT this site's feed
           https://www.cocosapartments.com/api/calendar.ics
         so a direct booking blocks those dates on the platforms too.
         (This closes the double-booking loop in both directions.)
         The feed is already live and serves valid iCal — it's just empty
         until the first paid direct booking, which is correct. Importing an
         empty calendar is fine and is the right time to do it.
         It publishes ONLY direct bookings, never the platforms' own blocks,
         so there is no echo loop between Airbnb and Booking.com.

[x] 3.4  Sanity-check on the live site once 3.2b is deployed: open the
         Availability section and confirm the nights above are greyed out and
         that picking a stay that runs THROUGH a booked block is impossible.
         DATA LAYER CONFIRMED (2026-07-23): the /api/availability payload carries
         the correct blocked ranges + disabledDates. What remains is the BROWSER
         eyeball on the rendered calendar — a human step (can't be done here).

NOTE (live-data finding, 2026-07-23): the feed currently contains a ~368-night
block 2027-01-20 → 2028-01-23 coming from one of the platform feeds. If it's
intentional (long-term let / closed season / dates not yet opened) it's correct;
if not, it's a stale event in Airbnb or Booking.com silently killing a year of
availability — check both platforms' Jan-2027 calendars and delete it there
(clears here within ~30 min feed cache).

NOTE (not a checklist item, just so it isn't a surprise): the Airbnb and
Booking.com calendars are not synced to each other — Airbnb's reservations do
not close those dates on Booking.com. This site takes the union of both, so it
is stricter than either platform. That is the safe direction, but it means the
platforms can still double-book each other independently of this site.

------------------------------------------------------------------------
STAGE 4 — SITE + SECRETS
------------------------------------------------------------------------
[x] 4.1  NEXT_PUBLIC_SITE_URL = https://www.cocosapartments.com — SET in the
         Vercel Production environment and deployed (2026-07-23). The live
         robots.txt + sitemap now use the real domain (verified:
         curl -s https://www.cocosapartments.com/robots.txt shows
         "Sitemap: https://www.cocosapartments.com/sitemap.xml", not the
         your-site.vercel.app placeholder). This also fixes the host in the
         owner "Approve & send payment link" button, so 6.4 is now testable.
[ ] 4.2  BOOKING_APPROVAL_SECRET = a long random value.
           Generate with:  openssl rand -base64 32
[ ] 4.3  (Optional) NEXT_PUBLIC_ANALYTICS_DOMAIN for cookieless Plausible stats.
[ ] 4.4  (Optional) NEXT_PUBLIC_GOOGLE_MAPS_KEY for the branded static map
         (restrict the key to your domain via HTTP-referrer in Google Cloud).

------------------------------------------------------------------------
STAGE 5 — PAYMENTS: VIVA DEMO (get the full flow working before real money)
------------------------------------------------------------------------
[x] 5.1  In the Viva DEMO portal, get Smart Checkout credentials:
           VIVA_CLIENT_ID, VIVA_CLIENT_SECRET   (Settings > API Access)
[x] 5.2  Create/note a payment SOURCE and its numeric code:
           VIVA_SOURCE_CODE   (Sales > Websites/Apps)
[ ] 5.3  On that source, set redirect URLs:            <-- NOT YET CONFIRMED
           Success -> https://www.cocosapartments.com/booking/success
           Failure -> https://www.cocosapartments.com/booking/failure
         (Use the www. host — the bare domain 308-redirects to www, which can
         break strict redirect matching.)
[x] 5.4  Set  VIVA_ENV = demo  and  VIVA_WEBHOOK_VERIFICATION_KEY = (from Viva).
         Verification key retrieved via
           GET https://demo.vivapayments.com/api/messages/config/token
           (Basic auth: Merchant ID as username, API Key as password — NOT
           demo-api.vivapayments.com, that host 404s on this endpoint).
[x] 5.5  Add a webhook in Viva pointing to
           https://www.cocosapartments.com/api/viva/webhook
         Event type: "Transaction Payment Created" (id 1796). Saved and
         verified successfully once VIVA_WEBHOOK_VERIFICATION_KEY was set +
         deployed and the www. URL was used (the bare domain redirect made
         Viva's verifier fail).
[ ] 5.6  DEPOSIT (pre-auth) capture — needed to release/capture the damage hold
         FROM the site (otherwise you resolve holds in the Viva portal):
           VIVA_MERCHANT_ID, VIVA_API_KEY  (Payment API Basic-auth credentials)
         Env vars are set, but double check in Viva: Settings > API Access >
           "Allow recurring payments and pre-auth captures via API"
         is actually TICKED — this was seen unchecked earlier and not
         explicitly re-confirmed after.        <-- VERIFY THIS CHECKBOX
         Deposit amount lives in content/siteConfig.ts (deposit.amountEur).

------------------------------------------------------------------------
STAGE 6 — DEPLOY + END-TO-END TEST (on DEMO)
Viva webhooks and redirects cannot reach localhost, so this must be on the
deployed HTTPS URL.
------------------------------------------------------------------------
[x] 6.1  Deploy to Vercel; point your domain at it (HTTPS auto).
[ ] 6.2  Open the site; confirm it loads, EL/EN toggle works, calendar shows
         availability, and the map/analytics render (if configured).
         UNBLOCKED (3.2b deployed) — /api/availability now serves the real
         blocked ranges, so the calendar no longer renders fully open. Only the
         human visual pass remains.
[x] 6.3  Submit a booking request (tick the consent box). Confirm:
           - you (owner) get the request email WITH an "Approve" button;
           - the guest gets an acknowledgement email.
         Confirmed working (test request from cocosapartments.com).
[ ] 6.4  Click Approve > Confirm. Confirm the guest gets a Viva pay link.
[ ] 6.5  Pay with a Viva DEMO test card. Confirm:
           - you land on /booking/success;
           - owner gets "PAID" email (with a "Send deposit hold link" button);
           - guest gets a receipt;
           - those dates now show as blocked on the site calendar.
[ ] 6.6  Deposit: click "Send deposit hold link", authorize the hold as the
         guest, then use the "Release / Capture" email link to RELEASE it.
         Confirm the guest gets the release email.
[ ] 6.7  Abuse checks: submit the request form ~6 times fast -> you should get
         a "too many requests" message (rate limiting works).

------------------------------------------------------------------------
STAGE 7 — GO LIVE (switch DEMO -> PRODUCTION Viva)
------------------------------------------------------------------------
[ ] 7.1  Repeat Stage 5 with LIVE Viva credentials (production portal).
[ ] 7.2  Set VIVA_ENV = production and swap in the live VIVA_* values +
         VIVA_MERCHANT_ID / VIVA_API_KEY + live webhook + redirect URLs.
[ ] 7.3  Redeploy.
[ ] 7.4  Do ONE small REAL transaction end-to-end; confirm the PAID email +
         receipt + calendar block; then refund it from the Viva portal.

------------------------------------------------------------------------
STAGE 8 — LEGAL / COMPLIANCE  (do NOT skip — this is real risk)
------------------------------------------------------------------------
[x] 8.1  ΑΜΑ (00003559420) + administrator name are set and shown in the footer.
[ ] 8.2  Have a LAWYER review /legal/privacy, /legal/terms, /legal/house-rules.
         Resolve every ⟪TODO⟫ marker in content/legal.ts (controller details,
         processor agreements + server locations, liability, cancellations).
[ ] 8.3  Confirm with an accountant the TAX record retention period (Greek law)
         and how you invoice STR income; set registration.taxId if invoicing
         as a business.
[ ] 8.4  When 8.2 + 8.3 are done, set siteConfig.legalDraft = false (hides the
         "draft — pending legal review" banner) and redeploy.
[ ] 8.5  Confirm the cancellation policy text on the site matches your real
         policy (it drives refunds you issue manually in Viva).

------------------------------------------------------------------------
STAGE 9 — POST-LAUNCH OPS / MONITORING
------------------------------------------------------------------------
[ ] 9.1  Watch the deployment logs for the first few real bookings. Look for
         lines tagged [viva/webhook], [audit], and any "amount mismatch" or
         "unmatched payment" owner alerts (those mean review in Viva, NOT paid).
[ ] 9.2  Set a personal reminder to RESOLVE the deposit hold (release/capture)
         within ~24h of each checkout — pre-auth holds expire on their own in a
         few days and CANNOT be captured after that.
[ ] 9.3  Keep BOOKING_APPROVAL_SECRET and all VIVA_*/RESEND/KV secrets out of
         git (they live only in the host env; .env.local is gitignored).
[ ] 9.4  Have a plan for GDPR requests (access/erasure) to controllerEmail.

------------------------------------------------------------------------
STAGE 10 — OPTIONAL / RECOMMENDED NEXT (Phase 4 — not blockers)
------------------------------------------------------------------------
[ ] 10.1 Guest EMAIL VERIFICATION before the owner approves (reduces fake/typo
         bookings and stolen-card risk).
[ ] 10.2 Automatic near-checkout DEPOSIT REMINDER (so 9.2 isn't manual).
[ ] 10.3 A privacy-friendly CAPTCHA (Cloudflare Turnstile) on the request form,
         only if you actually see spam/abuse.
[ ] 10.4 Optional: phone verification, smart-lock code automation on payment.

========================================================================
 WHEN STAGES 0–9 ARE COMPLETE YOU ARE READY TO ACCEPT REAL BOOKINGS.
 Stage 10 items are enhancements, not blockers.
 The two things that are NOT under the code's control and MUST be done by a
 human: Stage 8 (lawyer + accountant sign-off) and passing the Stage 6/7 tests.
========================================================================
